<form method="post" action="{{ path('fitxategia_delete', {'id': fitxategia.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fitxategia.id) }}">
    <button class="btnDeleteButton btn btn-xs btn-danger"><i class="fas fa-trash-alt"></i></button>
</form>
