
    <table class="table table-bordered table-striped table-hover text-nowrap">
        <thead>
            <tr>
                {#<th>Id</th>#}
                <th>Name</th>
                <th>Aurrekontua</th>
                <th>Aurrekontua Iva Gabe</th>
                <th>Sinadura</th>
                <th>Iraupena</th>
                <th>Adjudikazioa</th>
                <th>Adjudikazioa Iva Gabe</th>
                <th>Amaitua</th>
                <th>Luzapena</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for kontratua_lote in kontratua_lotes %}
            <tr>
                {#<td>{{ kontratua_lote.id }}</td>#}
                <td>{{ kontratua_lote.name }}</td>
                <td class="text-right">{{ kontratua_lote.aurrekontuaIva | format_currency('EUR') }}</td>
                <td class="text-right">{{ kontratua_lote.aurrekontuaIvaGabe | format_currency('EUR') }}</td>
                <td>{{ kontratua_lote.sinadura ? kontratua_lote.sinadura|date('Y-m-d') : '' }}</td>
                <td>{{ kontratua_lote.iraupena }}</td>
                <td class="text-right">{{ kontratua_lote.adjudikazioaIva | format_currency('EUR')  }}</td>
                <td class="text-right">{{ kontratua_lote.adjudikazioaIvaGabe | format_currency('EUR')  }}</td>
                <td>{% if kontratua_lote.amaitua %}<span class="badge badge-success">Bai</span>{% else %}<span class="badge badge-danger">Ez</span>{% endif %}</td>
                <td>{{ kontratua_lote.luzapena }}</td>
                <td class="text-center">
                    <ul class="list-inline">
                        <li class="list-inline-item">
{#                            <a class="btn btn-xs btn-primary" href="{{ path('saila_edit', {'id': saila.id}) }}"><i class="fas fa-edit"></i></a>#}
{#                            <a class="btnLoteEditInModal" href="{{ path('kontratua_lote_edit', {'id': kontratua_lote.id}) }}"><i class="fas fa-edit"></i></a>#}
                            <a data-id="{{ kontratua_lote.id }}" class="btnLoteEditInModal" href="#"><i class="fas fa-edit"></i></a>
                        </li>
                        <li class="list-inline-item">
                            {{ include('kontratua_lote/_delete_form.html.twig') }}
                        </li>
                        <li>
                            <a href=""></a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="10">{{ "no records found" | trans}}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
