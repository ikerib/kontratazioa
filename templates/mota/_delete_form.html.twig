<form method="post" action="{{ path('mota_delete', {'id': motum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ motum.id) }}">
    <button class="btn">Delete</button>
</form>
