<form method="post" action="{{ path('kontratua_delete', {'id': kontratua.id}) }}" >
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kontratua.id) }}">
    <button class="btnDeleteButton btn btn-xs btn-danger"><i class="fas fa-trash-alt"></i></button>
</form>
